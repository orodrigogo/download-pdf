import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  container: {
    backgroundColor: "#000",
    width: 150,
    height: 54,
    borderRadius: 7,
    justifyContent: "center",
    alignItems: "center",
  },
  title: {
    color: "#FFF",
    fontSize: 16,
    fontWeight: "bold",
  },
})
